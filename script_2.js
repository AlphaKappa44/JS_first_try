// Un prompt s'affiche avec la question suivante
question = prompt("De quel nombre veux-tu calculer la factorielle?");

// Utilisateur rentre un nombre (par ex 4)
fonctionFactorielle = (number * number-1);

// Dans la console le résultat est affiché
for(let count = number) {
  let number = prompt('De quel nombre veux-tu calculer la factorielle?');
  number = (number * (number-1));
  console.log(number `Le résultat est ${fonctionFactorielle}`);
}
